# Serverless runtime dependencies. The API talks to Supabase over its REST
# interface using the standard library, so no database driver or ORM is needed.
fastapi>=0.115.0,<1.0.0
pydantic>=2.0.0,<3.0.0
